# Core scientific stack
numpy>=1.24,<2.1
pandas>=2.0,<2.3
scipy>=1.11,<2.0

# Plotting
matplotlib>=3.7,<4.0
seaborn>=0.13,<0.14

# Classical ML and stats
scikit-learn>=1.3,<2.0
statsmodels>=0.14,<0.15
arch>=6.3,<9.0                  # GARCH, ARFIMA, MCS bootstrap (Hansen-Lunde-Nason)

# Deep learning for LSTM / NARX
torch>=2.1,<3.0                 # PyTorch (cross-platform; works on Apple Silicon via MPS)

# IO & utilities
pyarrow>=14.0,<24.0             # parquet read/write
tqdm>=4.66,<5.0

# Notebook
jupyterlab>=4.0,<5.0
ipykernel>=6.25
